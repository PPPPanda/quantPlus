# Phase 3: 改动清单 (Backlog)

## 目标
降低节假日跳空对缠论分型的结构性影响，改善 p2401 等高风险合约的收益。

## 约束
- p2601（标杆）不退化：Sharpe ≥ 2.5，ret ≥ 10%
- 总 PnL 不下降（iter14 基线：13712.2 pts）

---

## Backlog（按优先级排序）

### 🥇 优先级 1：调整阈值参数（低改动半径）

| ID | 方案 | 改动 | 缠论依据 | 预期效果 | 风险 |
|----|------|------|---------|---------|------|
| **S26c** | 阈值优化 | `gap_tier1_atr: 10→6`, `gap_tier2_atr: 30→15` | 更精准地识别危险的中等跳空 | 中等跳空冷却时间从3bar变为6bar | 可能过于敏感 |
| **S26d** | 延长冷却 | `gap_cooldown_bars: 6→10` | 给结构更多稳定时间 | 减少伪分型后的错误信号 | 可能错过机会 |

**代码改动**：
```python
# iter14基线
gap_extreme_atr: float = 1.5
gap_cooldown_bars: int = 6
gap_tier1_atr: float = 10.0
gap_tier2_atr: float = 30.0

# S26c 优化方案
gap_extreme_atr: float = 1.5      # 不变
gap_cooldown_bars: int = 6        # 不变
gap_tier1_atr: float = 6.0        # 从10降至6
gap_tier2_atr: float = 15.0       # 从30降至15
```

---

### 🥈 优先级 2：跳空后重置包含方向（中改动半径）

| ID | 方案 | 改动 | 缠论依据 | 预期效果 | 风险 |
|----|------|------|---------|---------|------|
| **S27** | 重置包含方向 | 跳空检测后设置 `_inclusion_dir = 0` | 缠论要求方向由市场自然推导，不能被历史污染 | 避免错误的包含合并 | 可能产生更多K线 |

**代码改动**（在 `_process_5m_bar` 中 S26b 逻辑后添加）：
```python
# S26b 分级冷却
if gap_atr >= self.gap_extreme_atr:
    ...
    if cooldown > 0:
        self._gap_cooldown_remaining = cooldown
        # S27: 重置包含方向
        self._inclusion_dir = 0
        self.write_log(f"[S27] 跳空后重置包含方向")
```

---

### 🥉 优先级 3：跳空后分型二次确认（中改动半径）

| ID | 方案 | 改动 | 缠论依据 | 预期效果 | 风险 |
|----|------|------|---------|---------|------|
| **S28** | 分型确认 | 跳空冷却期内的分型标记为"待确认"，需后续K线验证 | 缠论分型需要左右K线共同确认 | 过滤伪分型 | 延迟入场 |

**实现思路**：
1. 在 `_bi_points` 中添加 `confirmed: bool` 字段
2. 跳空冷却期内产生的分型设为 `confirmed=False`
3. 冷却结束后检查：若后续走势验证了该分型方向，则确认；否则删除

---

### 📋 优先级 4：根据合约风险评分调整冷却（高改动半径）

| ID | 方案 | 改动 | 缠论依据 | 预期效果 | 风险 |
|----|------|------|---------|---------|------|
| **S29** | 合约适应 | 高风险合约(p2401)冷却时间 +50% | 不同合约的波动率特征不同 | 针对性降低高风险合约的跳空影响 | 增加参数复杂度 |

**注意**：这需要建立合约-风险评分映射表，增加维护成本。优先考虑 S26c-S28。

---

## Phase 4 实施计划

### 小循环 1：S26c（阈值优化）
1. 改动：`gap_tier1_atr=6, gap_tier2_atr=15`
2. 回测：13 合约完整回测
3. 对比：改动前 vs 改动后

### 小循环 2：S26c + S27（阈值优化 + 重置包含方向）
1. 在小循环 1 基础上添加 S27
2. 回测：13 合约
3. 如果效果不佳，回退到小循环 1

### 小循环 3：备选方案（如果前两轮效果不佳）
- S26d（延长冷却期）
- S28（分型二次确认）

---

## 预期结果

| 指标 | iter14 基线 | 预期目标 |
|------|-------------|----------|
| TOTAL PnL | 13712.2 | ≥13712.2（不退化） |
| p2401 | -672.9 | ≥-400（改善 40%） |
| p2601 Sharpe | 2.50+ | ≥2.50（不退化） |

---

## 决策门 3

✅ Backlog 输出完毕
✅ 按优先级排序：S26c > S27 > S26d > S28 > S29
